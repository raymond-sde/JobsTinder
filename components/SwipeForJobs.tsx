import * as React from "react";
import { Text, View } from "react-native";

export const SwipeForJobs = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Swipe for Jobs!</Text>
    </View>
  );
}
